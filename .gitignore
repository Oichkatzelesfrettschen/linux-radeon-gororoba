# Generated register tables: the build derives these from reg_srcs/ through
# mkregtable, so a tracked copy would let a stale artifact outrank its source.
*_reg_safe.h

# Host program built from mkregtable.c during the build.
/drivers/gpu/drm/radeon/mkregtable

# Kbuild output.
*.o
*.ko
*.mod
*.mod.c
*.cmd
.tmp_versions/
Module.symvers
modules.order
