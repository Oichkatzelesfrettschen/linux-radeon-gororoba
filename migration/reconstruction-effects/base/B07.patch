diff --git a/radeon_gem.c b/radeon_gem.c
index f86773f..9ccc9f0 100644
--- a/radeon_gem.c
+++ b/radeon_gem.c
@@ -86,7 +86,11 @@ static void radeon_gem_object_free(struct drm_gem_object *gobj)
 
 	if (robj) {
 		radeon_mn_unregister(robj);
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(7, 0, 0)
+		ttm_bo_fini(&robj->tbo);
+#else
 		ttm_bo_put(&robj->tbo);
+#endif
 	}
 }
 
